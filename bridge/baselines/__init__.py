"""Retrieval-framework baselines: FLARE and Self-RAG.

    flare     FLARE confidence-triggered retrieval loop (HuggingFace models)
    selfrag   Self-RAG reflection-token adaptive retrieval (vLLM)
    protocol  runs either framework through the behavioral tests and writes
              predictions in the bridge.evaluation format
"""
