# Tested with Python 3.10, CUDA 12.4, torch 2.4, transformers 5.2, peft 0.18,
# accelerate 1.13, numpy 1.26, scikit-learn 1.6, matplotlib 3.7.
torch>=2.1
transformers>=4.40
peft>=0.10
accelerate>=0.28
numpy>=1.24,<2
scikit-learn>=1.3
matplotlib>=3.7
# FLARE baseline only
nltk>=3.8
# The Self-RAG baseline also needs vllm; install it in a separate environment,
# since it pins its own torch build.
